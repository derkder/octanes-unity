在 Unity 中，`transform.localRotation` 属性表示的是物体相对于父物体坐标系的旋转，也就是局部坐标系的旋转。这个属性返回的是一个四元数。

如果要设置物体的局部旋转，可以直接修改 `transform.localRotation` 属性，例如：

```csharp
transform.localRotation = Quaternion.Euler(xAngle, yAngle, zAngle);
