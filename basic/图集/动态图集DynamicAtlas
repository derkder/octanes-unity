定义：
    在运行时动态生成的图集


应用背景：
    解决游戏中动态图片太多的问题，也就是我们没有办法预先放在UI上的。
    例如moba里每个队伍的阵营是不确定的，左上角的英雄头像是动态加载，而且由于技能之类的图片太多，所以没有办法打成一张静态图集。而如果作为独立图片动态加载，就会多十几个DrawCall。即便是打成多张静态图集，也会导致UI渲染的批次被打断。


原理：
    


使用方法：
  https://github.com/tkonexhh/DynamicAtlas/tree/master

