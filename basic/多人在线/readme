RPC:[前后端通信里]
  RPC（Remote Procedure Call）是一种用于分布式系统中的通信协议，它允许一个计算机程序通过网络请求另一个计算机上的程序执行某些操作。通俗地说，RPC就是像调用本地函数一样调用远程函数。

  RPC的工作原理是，客户端调用远程服务端提供的函数，然后将请求传输给服务端，服务端执行请求的操作，并将结果返回给客户端。在这个过程中，客户端和服务端之间的通信可以通过不同的协议来实现，
比如TCP/IP协议。
